// ChartJS Graphs

// eslint-disable-next-line
const [chartMainColor, chartSecondColor, chartThirdColor] =
    configuration.COLOR_STACKED_BAR_CHARTS;

// eslint-disable-next-line
const chartHoverMainColor = chartSecondColor;